# Backend

- Node.js + Express
- MongoDB (with Mongoose)

## Setup

1. Copy `.env.example` to `.env` and fill in your secrets.
2. Run `npm install`.
3. To start: `node src/app.js` (after app.js is created, see later steps).
